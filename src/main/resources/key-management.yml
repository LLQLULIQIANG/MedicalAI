# 统一密钥管理：仓库内不写真实密钥，请在环境变量中配置。
#
# 1) 阿里云百炼 / DashScope — https://dashscope.console.aliyun.com/
# 2) SearchAPI — https://www.searchapi.io/
# 3) 高德开放平台 — https://lbs.amap.com/
app:
  keys:
    dashscope: ${DASHSCOPE_API_KEY:}
    searchapi: ${SEARCHAPI_API_KEY:}
    amap: ${AMAP_MAPS_API_KEY:}
